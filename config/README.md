# 配置文件

此文件夹包含配置文件。

请根据需要补充：

- `.env` (环境变量，需安装 flutter_dotenv)
  内容示例：
  ```
  OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxx
  STEPFUN_API_KEY=xxxxxxxxxxxxxxxx
  ```

- `app_config.dart` (应用配置类)
  可存放默认 API 端点、超时时间等

当前为空。